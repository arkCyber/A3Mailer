//! Pattern matching module

/// Pattern matcher
pub struct PatternMatcher;

/// Threat pattern
pub struct ThreatPattern;

impl PatternMatcher {
    /// Create new pattern matcher
    pub fn new() -> Self {
        Self
    }
}
