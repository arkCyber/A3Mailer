//! Template engine

/// Template engine
pub struct TemplateEngine;

/// Template
pub struct Template;

/// Template context
pub struct TemplateContext;

impl TemplateEngine {
    /// Create new template engine
    pub fn new() -> Self {
        Self
    }
}
