//! Kubernetes controllers

/// Mail server controller
pub struct MailServerController;

/// Controller context
pub struct ControllerContext;

impl MailServerController {
    /// Create new controller
    pub fn new() -> Self {
        Self
    }
}
