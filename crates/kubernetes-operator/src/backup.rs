//! Backup controller module

/// Backup controller
pub struct BackupController;

/// Backup policy
pub struct BackupPolicy;

/// Restore operation
pub struct RestoreOperation;

impl BackupController {
    /// Create new backup controller
    pub fn new() -> Self {
        Self
    }
}
